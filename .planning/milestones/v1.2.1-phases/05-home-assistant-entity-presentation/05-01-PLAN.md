---
phase: 05-home-assistant-entity-presentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_entity_presentation_contract.py
autonomous: true
requirements:
  - HAPR-01
  - HAPR-02
must_haves:
  truths:
    - "User always sees exactly three ZSE HDO entities after integration setup."
    - "Entity identities remain stable across reloads for the same HDO number."
    - "Dashboard/automation-critical attributes stay present on entity payloads."
  artifacts:
    - path: tests/test_entity_presentation_contract.py
      provides: "Presentation contract tests for entity count, IDs, unique_id patterns, and required attributes."
      min_lines: 80
  key_links:
    - from: tests/test_entity_presentation_contract.py
      to: custom_components/zse_hdo/sensor.py
      via: "entity instantiation + attribute assertions"
      pattern: "ZSEHDOTariffSensor|ZSEHDONextSwitchSensor|ZSEHDOTodayScheduleSensor"
---

<objective>
Freeze the Home Assistant entity presentation contract so future refactors cannot silently break IDs or required attributes.

Purpose: Protect HAPR-01/HAPR-02 behavior shipped in v1.1.0 by converting implicit expectations into enforced tests.
Output: Contract test module covering 3-entity surface, stable identifiers, and required attribute keys.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-home-assistant-entity-presentation/05-RESEARCH.md
@.planning/phases/05-home-assistant-entity-presentation/05-PATTERNS.md
@custom_components/zse_hdo/sensor.py
</context>

<read_first>
@custom_components/zse_hdo/sensor.py
@tests/test_sensor_staleness_attrs.py
@tests/test_coordinator_reliability.py
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create entity presentation contract test scaffold</name>
  <files>tests/test_entity_presentation_contract.py</files>
  <behavior>
    - Test exposes deterministic coordinator payload fixture and sensor module loader mirroring existing test style.
    - Test instantiates the three entity classes for one fixed HDO number.
  </behavior>
  <action>Create `tests/test_entity_presentation_contract.py` using the same dynamic module loading/test harness pattern as existing sensor/coordinator tests. Include shared payload fixture with required fields (`current_tariff`, schedule lists, reliability metadata) and helper that builds all three entity instances for HDO 145 per HAPR-01/HAPR-02.</action>
  <verify>
    <automated>py -m pytest -q tests/test_entity_presentation_contract.py::test_three_entities_surface</automated>
  </verify>
  <done>Test module exists and can instantiate all three entities without importing Home Assistant runtime internals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce entity surface and identifier stability contract</name>
  <files>tests/test_entity_presentation_contract.py</files>
  <behavior>
    - Exactly one binary sensor and two sensors are asserted for the integration surface.
    - `unique_id` format is fixed to `zse_hdo_145_tariff|next_switch|today_schedule`.
    - Required attribute keys exist for each entity type.
  </behavior>
  <action>Add focused contract tests that assert entity class count/domain mix, exact `unique_id` naming scheme, and required minimum attribute keys (`tariff_name`, `time`, `to_tariff_name`, `periods`, `period_count`, reliability keys) per HAPR-01/HAPR-02. Keep assertions additive-friendly (required-minimum set), not strict full-dict snapshots.</action>
  <verify>
    <automated>py -m pytest -q tests/test_entity_presentation_contract.py -k "surface or unique_id or attrs"</automated>
  </verify>
  <done>Contract tests fail on ID drift or attribute key removals and pass on current implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused regression gate for presentation contracts</name>
  <files>tests/test_entity_presentation_contract.py</files>
  <action>Execute the full new contract test file and fix any fixture or assertion mismatch until green. Do not change integration behavior in this plan; only align test expectations with shipped contract.</action>
  <verify>
    <automated>py -m pytest -q tests/test_entity_presentation_contract.py</automated>
  </verify>
  <done>New contract suite is green and ready to protect Phase 5 surface in future changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> entity contract | Untrusted future code changes can remove/rename entity keys or IDs silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `custom_components/zse_hdo/sensor.py` entity IDs/attrs | mitigate | Add explicit contract tests for exact `unique_id` patterns and required attribute keys. |
| T-05-02 | I | dashboard templates consuming `state_attr()` | mitigate | Require reliability and presentation keys in tests to prevent silent metadata disappearance. |
</threat_model>

<verification>
- `py -m pytest -q tests/test_entity_presentation_contract.py`
</verification>

<acceptance_criteria>
- HAPR-01: three entities are asserted and protected by automated tests.
- HAPR-02: stable `unique_id` patterns and required dashboard attributes are enforced by automated tests.
</acceptance_criteria>

<success_criteria>
Entity presentation contract is machine-checked and blocks regressions in IDs/required attributes before release.
</success_criteria>

<output>
After completion, create `.planning/phases/05-home-assistant-entity-presentation/05-01-SUMMARY.md`
</output>
