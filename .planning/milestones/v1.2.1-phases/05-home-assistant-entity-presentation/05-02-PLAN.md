---
phase: 05-home-assistant-entity-presentation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/test_entity_presentation_contract.py
  - README.md
  - EXAMPLES.md
autonomous: false
requirements:
  - HAPR-02
  - HAPR-03
must_haves:
  truths:
    - "Users can apply a built-in Entities card recipe without extra custom cards."
    - "Advanced Mushroom/card-mod examples only reference attributes that truly exist."
    - "Documentation stays aligned with current entity contract after updates."
  artifacts:
    - path: tests/test_entity_presentation_contract.py
      provides: "Docs/examples parity tests against live entity attributes."
      min_lines: 130
    - path: README.md
      provides: "Canonical entity and baseline Lovelace guidance."
      contains: "Entity IDs and required attributes"
    - path: EXAMPLES.md
      provides: "Baseline Entities card + optional advanced recipes."
      contains: "Mushroom/card-mod optional path"
  key_links:
    - from: README.md
      to: custom_components/zse_hdo/sensor.py
      via: "documented attribute names"
      pattern: "tariff_name|to_tariff_name|period_count|is_stale"
    - from: EXAMPLES.md
      to: tests/test_entity_presentation_contract.py
      via: "docs-parity assertions for referenced attrs"
      pattern: "state_attr\\("
---

<objective>
Harden docs and examples so the Phase 5 presentation contract remains accurate for both baseline and advanced dashboard usage.

Purpose: Deliver HAPR-03 parity guarantees and prevent support regressions caused by docs/example drift.
Output: Docs-parity tests plus synchronized `README.md` and `EXAMPLES.md` references.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-home-assistant-entity-presentation/05-RESEARCH.md
@.planning/phases/05-home-assistant-entity-presentation/05-PATTERNS.md
@README.md
@EXAMPLES.md
@tests/test_entity_presentation_contract.py
</context>

<read_first>
@README.md
@EXAMPLES.md
@custom_components/zse_hdo/sensor.py
@tests/test_entity_presentation_contract.py
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add docs/examples parity assertions to contract suite</name>
  <files>tests/test_entity_presentation_contract.py</files>
  <behavior>
    - README documented keys are asserted against live entity attribute payloads.
    - EXAMPLES `state_attr()` usages are validated as present keys for referenced entities.
  </behavior>
  <action>Extend the contract test file from 05-01 with a docs-parity test that verifies every required presentation/reliability attribute referenced in `README.md` and advanced examples in `EXAMPLES.md` exists on corresponding entities (HAPR-03, HAPR-02). Use required-minimum parity rather than exact text snapshot matching to allow additive docs growth.</action>
  <verify>
    <automated>py -m pytest -q tests/test_entity_presentation_contract.py::test_docs_examples_attribute_parity</automated>
  </verify>
  <done>Parity test fails when docs/examples reference non-existent keys and passes on aligned docs.</done>
</task>

<task type="auto">
  <name>Task 2: Align README and EXAMPLES baseline/advanced contract narrative</name>
  <files>README.md, EXAMPLES.md</files>
  <action>Update documentation so baseline built-in Entities card path is clearly first-class, while Mushroom/card-mod remains optional advanced guidance. Ensure all listed entity IDs and attribute names exactly match sensor contract keys, including reliability metadata and next-switch naming. Preserve Slovak user-facing wording style.</action>
  <verify>
    <automated>py -m pytest -q tests/test_entity_presentation_contract.py -k "docs or parity"</automated>
  </verify>
  <done>README and EXAMPLES are contract-accurate and consistent with test-enforced entity schema.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human dashboard parity verification</name>
  <what-built>Automated docs/examples parity checks and synchronized dashboard guidance for baseline + advanced cards.</what-built>
  <how-to-verify>
    1) Open Home Assistant Lovelace and add the baseline `type: entities` recipe from `EXAMPLES.md`.
    2) Confirm the three entities render without template errors and show meaningful states/attributes.
    3) If Mushroom/card-mod is installed, paste the advanced timeline card and confirm it renders without missing-attribute warnings.
  </how-to-verify>
  <action>Run a manual visual verification pass in a Home Assistant dashboard after automated tests are green, confirming baseline and optional advanced examples both match documented behavior.</action>
  <verify>
    <automated>py -m pytest -q tests/test_entity_presentation_contract.py -k parity</automated>
  </verify>
  <done>User confirms dashboard examples render correctly or provides precise mismatch feedback for follow-up fixes.</done>
  <resume-signal>Type "approved" if visuals/behavior match docs, or report exact mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/examples -> user dashboards | Users trust documented keys/templates; drift causes runtime template failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | `README.md` / `EXAMPLES.md` attribute references | mitigate | Add automated docs-parity test tied to real entity payload keys. |
| T-05-04 | I | Lovelace templates using `state_attr()` | mitigate | Keep baseline + advanced examples synchronized and manually verify dashboard rendering checkpoint. |
</threat_model>

<verification>
- `py -m pytest -q tests/test_entity_presentation_contract.py`
- `py -m pytest -q tests`
</verification>

<acceptance_criteria>
- HAPR-03: docs and examples are parity-validated against live entity schema.
- HAPR-02: documented metadata fields remain aligned with stable entity attribute contract.
</acceptance_criteria>

<success_criteria>
Phase 5 presentation docs cannot drift from implementation without failing automated parity checks, and baseline dashboard guidance remains usable without custom cards.
</success_criteria>

<output>
After completion, create `.planning/phases/05-home-assistant-entity-presentation/05-02-SUMMARY.md`
</output>
