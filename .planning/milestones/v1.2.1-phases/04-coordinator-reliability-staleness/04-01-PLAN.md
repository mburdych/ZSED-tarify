---
phase: 04-coordinator-reliability-staleness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/zse_hdo/const.py
  - custom_components/zse_hdo/coordinator.py
  - tests/test_coordinator_reliability.py
autonomous: true
requirements:
  - RELI-01
  - RELI-02
  - RELI-03
must_haves:
  truths:
    - "Coordinator applies explicit retry/backoff after runtime refresh failures instead of hammering the source."
    - "Cached schedule remains available during source outages with explicit degraded-mode metadata per D-01 and D-02."
    - "Reliability state is owned by coordinator as the single refresh/cache authority."
  artifacts:
    - path: "custom_components/zse_hdo/const.py"
      provides: "Central retry/backoff tuning constants with safe bounds."
    - path: "custom_components/zse_hdo/coordinator.py"
      provides: "Coordinator reliability state machine with stale metadata, fallback behavior, and retry scheduling."
    - path: "tests/test_coordinator_reliability.py"
      provides: "Regression tests for backoff progression, stale metadata, and cache fallback semantics."
  key_links:
    - from: "custom_components/zse_hdo/coordinator.py"
      to: "homeassistant.helpers.update_coordinator.UpdateFailed"
      via: "retry_after or scheduled retry lane"
      pattern: "UpdateFailed\\(|retry"
    - from: "custom_components/zse_hdo/coordinator.py"
      to: "self._last_known_data"
      via: "fallback return with reliability metadata enrichment"
      pattern: "_last_known_data"
    - from: "tests/test_coordinator_reliability.py"
      to: "custom_components/zse_hdo/coordinator.py"
      via: "failure/success path assertions"
      pattern: "consecutive_failures|is_stale|stale_for_s"
---

<objective>
Implement coordinator-level reliability controls so repeated runtime failures use explicit backoff while preserving unlimited cached availability with transparent stale-state metadata.

Purpose: Fulfill RELI-03 and the coordinator-owned reliability contract for RELI-01/RELI-02 without changing platform structure.
Output: Retry/backoff-capable coordinator payload with reliability metadata and automated coverage for degraded-mode behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-coordinator-reliability-staleness/04-CONTEXT.md
@.planning/phases/04-coordinator-reliability-staleness/04-RESEARCH.md
@.planning/phases/04-coordinator-reliability-staleness/04-PATTERNS.md
@custom_components/zse_hdo/const.py
@custom_components/zse_hdo/coordinator.py
</context>

<read_first>
- Implement locked decisions D-01 and D-02 exactly: unlimited cached fallback stays enabled and stale state is always explicit.
- Keep coordinator as single owner of refresh/cache/reliability logic (RELI-01); entities must consume metadata, not compute it.
- Do not add a new staleness entity or logs-only fallback (deferred + D-03/D-04 constraints).
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coordinator reliability contract tests before implementation</name>
  <files>tests/test_coordinator_reliability.py</files>
  <behavior>
    - Test 1: repeated refresh failures increment `consecutive_failures` and produce an explicit retry/backoff progression (RELI-03, per D-02).
    - Test 2: when `_last_known_data` exists, coordinator returns cached payload with `is_stale=true` and non-zero `stale_for_s` (RELI-02, per D-01/D-02).
    - Test 3: first successful runtime refresh resets failure counters and stale state fields for later recovery assertions (feeds D-05 validation in next plan).
  </behavior>
  <action>Create reliability-focused async tests using coordinator-level mocks (no HA UI harness), following analog `tests/test_parser_get_schedule.py` (async patching) and shared fallback pattern in `custom_components/zse_hdo/coordinator.py` from 04-PATTERNS. Include deterministic assertions for reliability metadata keys and bounded retry delay values. Mark tests that depend on sensor attribute propagation as out-of-scope for this file to keep ownership clean.</action>
  <verify>
    <automated>py -m pytest tests/test_coordinator_reliability.py -q -x</automated>
  </verify>
  <done>Coordinator reliability tests fail before implementation and assert explicit contract fields for retry/backoff and stale fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded retry/backoff and reliability metadata in coordinator</name>
  <files>custom_components/zse_hdo/const.py, custom_components/zse_hdo/coordinator.py</files>
  <action>Add reliability constants (min/max delay and multiplier) in `const.py` using the constant-table analog in `custom_components/zse_hdo/const.py` from 04-PATTERNS, then implement a coordinator-owned reliability envelope in `_async_update_data` with fields that include `is_stale`, `stale_for_s`, `last_success_at`, `last_error_at`, `consecutive_failures`, and `next_retry_at`. Follow coordinator analogs in `custom_components/zse_hdo/coordinator.py` (frequency branching, scheduled timer, cache fallback) from 04-PATTERNS. On runtime failures: keep unlimited cached fallback per D-01, set stale metadata per D-02, and apply explicit backoff for interval and scheduled modes. On first success after failures: reset stale/failure metadata at source so downstream entities can clear state automatically (precondition for D-05).</action>
  <verify>
    <automated>py -m pytest tests/test_coordinator_reliability.py -q -x</automated>
  </verify>
  <done>Coordinator returns reliability-enriched payloads for both success and degraded paths and uses bounded retry/backoff instead of immediate repeated retries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote source -> coordinator refresh | untrusted network failures and timing hints enter retry logic |
| coordinator reliability state -> entity consumers | stale/fresh truth crosses into user-visible state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | D | `coordinator.py` retry loop | mitigate | Use bounded backoff with minimum/maximum delay to prevent retry storms under persistent failures. |
| T-04-02 | I | coordinator fallback payload | mitigate | Always set explicit stale metadata (`is_stale`, `stale_for_s`, timestamps, failures) when returning cached data. |
| T-04-03 | T | retry delay source | mitigate | Clamp computed retry values to configured numeric bounds before scheduling next attempt. |
</threat_model>

<verification>
- Targeted gate: `py -m pytest tests/test_coordinator_reliability.py -q -x`
- Safety gate: `py -m pytest tests -q`
</verification>

<success_criteria>
- RELI-03 is enforced by deterministic tests showing explicit retry/backoff behavior after runtime failures.
- RELI-02 behavior remains intact with unlimited cached fallback and explicit stale metadata.
- Reliability state ownership stays in coordinator, preserving RELI-01 boundaries for downstream entity work.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coordinator-reliability-staleness/04-01-SUMMARY.md`.
</output>
