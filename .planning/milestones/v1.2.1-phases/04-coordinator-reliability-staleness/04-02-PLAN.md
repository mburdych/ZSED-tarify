---
phase: 04-coordinator-reliability-staleness
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - custom_components/zse_hdo/coordinator.py
  - custom_components/zse_hdo/sensor.py
  - tests/test_sensor_staleness_attrs.py
autonomous: true
requirements:
  - RELI-04
  - RELI-02
must_haves:
  truths:
    - "All existing entities expose stale/fresh metadata directly in attributes per D-03 and D-04."
    - "After first successful refresh, stale flag and age reset automatically with no manual intervention per D-05."
    - "Entity availability remains stable while degraded-mode data is transparently marked stale."
  artifacts:
    - path: "custom_components/zse_hdo/sensor.py"
      provides: "Unified staleness attribute propagation across tariff, next-switch, and today-schedule entities."
    - path: "custom_components/zse_hdo/coordinator.py"
      provides: "Recovery reset semantics aligned with entity attribute contract."
    - path: "tests/test_sensor_staleness_attrs.py"
      provides: "Regression tests for stale metadata visibility and recovery reset in existing entities."
  key_links:
    - from: "custom_components/zse_hdo/coordinator.py"
      to: "custom_components/zse_hdo/sensor.py"
      via: "coordinator.data reliability keys mapped into entity attributes"
      pattern: "reliability|is_stale|stale_for_s"
    - from: "tests/test_sensor_staleness_attrs.py"
      to: "custom_components/zse_hdo/sensor.py"
      via: "attribute contract assertions"
      pattern: "extra_state_attributes"
    - from: "custom_components/zse_hdo/sensor.py"
      to: "existing entity unique_id contracts"
      via: "attribute-only extension with no new platform entities"
      pattern: "zse_hdo_.*_(tariff|next_switch|today_schedule)"
---

<objective>
Propagate coordinator staleness metadata into existing entity attributes and verify automatic recovery-state reset behavior.

Purpose: Deliver RELI-04 user-visible observability without introducing new entities, while preserving degraded-mode continuity.
Output: Entity attribute contract extension and automated tests proving stale visibility plus post-recovery clearing.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-coordinator-reliability-staleness/04-CONTEXT.md
@.planning/phases/04-coordinator-reliability-staleness/04-RESEARCH.md
@.planning/phases/04-coordinator-reliability-staleness/04-PATTERNS.md
@.planning/phases/04-coordinator-reliability-staleness/04-01-PLAN.md
@custom_components/zse_hdo/coordinator.py
@custom_components/zse_hdo/sensor.py
</context>

<read_first>
- Preserve D-03 and D-04: expose staleness only via existing entity attributes; never add a dedicated staleness sensor in this phase.
- Use coordinator metadata as source-of-truth (no entity-local stale timers) to keep RELI-01 ownership boundaries intact.
- Validate D-05 explicitly: first successful refresh after failures must clear stale markers and reset age.
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add entity-level staleness attribute contract tests</name>
  <files>tests/test_sensor_staleness_attrs.py</files>
  <behavior>
    - Test 1: tariff, next-switch, and today-schedule entities include stale metadata attributes when coordinator data is degraded (RELI-04, D-03/D-04).
    - Test 2: stale metadata is consistent across all three entities for the same coordinator payload (single source semantics).
    - Test 3: after simulated recovery payload, attributes show `is_stale=false` and reset age/failure values (D-05).
  </behavior>
  <action>Create focused sensor contract tests with coordinator payload fixtures that include fresh, stale, and recovered states, following test analog `tests/test_parser_normalize.py` (arrange-act-assert) and parameterization analog from `tests/test_parser_extract.py` documented in 04-PATTERNS. Assert presence and expected values for agreed metadata keys while preserving existing attributes (`hdo_number`, `rate_type`, `period_count`, etc.). Keep tests independent from network/parsing logic.</action>
  <verify>
    <automated>py -m pytest tests/test_sensor_staleness_attrs.py -q -x</automated>
  </verify>
  <done>Entity attribute tests fail before implementation and codify the RELI-04 stale observability contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire reliability metadata into existing sensor attributes and finalize recovery reset path</name>
  <files>custom_components/zse_hdo/sensor.py, custom_components/zse_hdo/coordinator.py</files>
  <action>Extend `extra_state_attributes` in all existing entities to include coordinator-provided reliability keys selected in 04-01 implementation (per D-03/D-04), using attribute projection analogs in `custom_components/zse_hdo/sensor.py` (including guarded empty-data pattern) from 04-PATTERNS. Keep backward-compatible existing attributes and do not alter entity platform registration. If needed, finalize coordinator success-path normalization using coordinator analog `custom_components/zse_hdo/coordinator.py` from 04-PATTERNS so recovery guarantees are explicit and consistent for attribute consumers (D-05), including zeroed/cleared stale fields on first successful post-failure refresh.</action>
  <verify>
    <automated>py -m pytest tests/test_sensor_staleness_attrs.py tests/test_coordinator_reliability.py -q -x</automated>
  </verify>
  <done>All existing entities expose stale metadata directly, and stale indicators clear automatically after successful recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator data -> entity attributes | reliability state becomes user-visible in Home Assistant |
| stale/recovery transitions -> automations/dashboard logic | attribute consumers rely on correctness of transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | I | `sensor.py` attribute projection | mitigate | Project explicit stale/fresh fields on all entities so users can detect degraded state directly (no logs-only ambiguity). |
| T-04-05 | T | recovery transition mapping | mitigate | Reset stale fields atomically on first success and assert via recovery tests to prevent contradictory metadata. |
| T-04-06 | R | user troubleshooting path | mitigate | Include failure counters and error timestamps in attributes to provide audit trail for stale periods. |
</threat_model>

<verification>
- Primary gate: `py -m pytest tests/test_sensor_staleness_attrs.py tests/test_coordinator_reliability.py -q -x`
- Full phase gate: `py -m pytest tests -q`
</verification>

<success_criteria>
- RELI-04 is delivered through existing entity attributes with no new sensor platform.
- D-05 recovery semantics are regression-tested: stale flag and age clear on first successful refresh.
- Degraded-mode availability remains preserved while stale state is obvious to users and automations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coordinator-reliability-staleness/04-02-SUMMARY.md`.
</output>
