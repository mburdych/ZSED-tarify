---
phase: 09-remaining-low-tariff-helper-entity
plan: 01
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - custom_components/zse_hdo/sensor.py
  - tests/test_low_tariff_remaining_sensor.py
  - tests/test_entity_presentation_contract.py
  - README.md
  - EXAMPLES.md
autonomous: false
requirements:
  - VADD-01
must_haves:
  truths:
    - "Uzivatel vidi zostavajuci cas low-tariff okna bez potreby template vypoctov."
    - "Helper entita je stabilna na hraniciach dna, vikend/pracovny den, a mimo low tarify."
    - "Novy vystup je docs/test kontraktovo pokryty bez regressie existujucich 3 entit."
  artifacts:
    - path: custom_components/zse_hdo/sensor.py
      provides: "Nova helper entita so stavom remaining low-tariff window a atributmi kontextu."
    - path: tests/test_low_tariff_remaining_sensor.py
      provides: "Boundary-focused tests pre low-window remaining kalkulus (weekday/weekend/midnight)."
    - path: tests/test_entity_presentation_contract.py
      provides: "Rozsireny contract gate pre novu helper entitu a required attrs."
    - path: README.md
      provides: "Canonical popis helper entity + baseline dashboard usage."
    - path: EXAMPLES.md
      provides: "Prakticky baseline priklad s helper entitou."
  key_links:
    - from: custom_components/zse_hdo/sensor.py
      to: custom_components/zse_hdo/time_semantics.py
      via: "reuse existing time semantics helpers"
      pattern: "calculate_next_switch|get_periods_for_datetime|is_low_tariff"
    - from: tests/test_low_tariff_remaining_sensor.py
      to: custom_components/zse_hdo/sensor.py
      via: "deterministic time-boundary assertions"
      pattern: "remaining|period_end|minutes"
    - from: README.md
      to: tests/test_entity_presentation_contract.py
      via: "docs/examples parity for new entity attrs"
      pattern: "remaining_low|state_attr\\("
---

<objective>
Implement VADD-01 by adding a helper entity that exposes remaining time in the active low-tariff window, including clear behavior when low tariff is inactive.

Purpose: Remove dashboard template complexity and give users instant operational insight for appliance timing.
Output: New helper sensor + regression-safe tests + docs parity updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-diagnostic-signal-separation/08-01-SUMMARY.md
@custom_components/zse_hdo/sensor.py
@custom_components/zse_hdo/time_semantics.py
@tests/test_entity_presentation_contract.py
</context>

<read_first>
@custom_components/zse_hdo/sensor.py
@custom_components/zse_hdo/time_semantics.py
@tests/test_entity_presentation_contract.py
@README.md
@EXAMPLES.md
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing boundary tests for remaining low-window helper</name>
  <files>tests/test_low_tariff_remaining_sensor.py</files>
  <behavior>
    - Active low tariff: helper returns positive remaining duration to period end.
    - Outside low tariff: helper returns zero/idle contract value with clear status attr.
    - Midnight-crossing low interval computes correct remaining time past 00:00.
    - Weekend/workday branch uses correct schedule list.
  </behavior>
  <action>Create dedicated tests with deterministic mocked `dt_util.now()` timestamps and fixture payloads mirroring current schedule schema. Cover at least one workday interval, one weekend interval, and one midnight-crossing interval.</action>
  <verify>
    <automated>py -m pytest -q tests/test_low_tariff_remaining_sensor.py -x</automated>
  </verify>
  <done>Tests fail before implementation and lock expected helper behavior across boundary cases.</done>
</task>

<task type="auto">
  <name>Task 2: Implement helper entity in sensor platform</name>
  <files>custom_components/zse_hdo/sensor.py</files>
  <action>Add a new CoordinatorEntity Sensor that computes remaining low-tariff window from existing schedule/time helpers without duplicating core tariff logic. Include stable `unique_id` naming (`zse_hdo_<N>_low_remaining`) and required attrs (`remaining_minutes`, `period_end`, `is_low_tariff_now`) plus reliability/diagnostic metadata passthrough.</action>
  <verify>
    <automated>py -m pytest -q tests/test_low_tariff_remaining_sensor.py -x</automated>
  </verify>
  <done>Helper entity reports deterministic remaining window state and reuses existing semantics layer safely.</done>
</task>

<task type="auto">
  <name>Task 3: Extend entity presentation contract and docs parity</name>
  <files>tests/test_entity_presentation_contract.py, README.md, EXAMPLES.md</files>
  <action>Update contract tests to include new helper entity and required attrs while preserving existing entity assertions. Synchronize README/EXAMPLES with baseline usage and optional advanced usage for helper entity so docs parity remains enforced.</action>
  <verify>
    <automated>py -m pytest -q tests/test_entity_presentation_contract.py -k "remaining or helper or parity" -x</automated>
  </verify>
  <done>Entity contract + docs parity gates include the helper sensor and prevent schema/docs drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual dashboard readability validation</name>
  <what-built>Remaining low-tariff helper sensor with docs/examples updates.</what-built>
  <how-to-verify>
    1) Pridaj helper entitu do baseline Entities card z `EXAMPLES.md`.
    2) Pocakaj na low-tariff a over, ze hodnota remaining klesa smerom k nule.
    3) Mimo low tarify over, ze helper ma idle/zero hodnotu bez chyb v sablonach.
  </how-to-verify>
  <action>Run one visual check in HA dashboard for active and inactive low-tariff states.</action>
  <verify>
    <automated>py -m pytest -q tests/test_low_tariff_remaining_sensor.py tests/test_entity_presentation_contract.py -x</automated>
  </verify>
  <done>User confirms helper readability and expected behavior or reports exact mismatch.</done>
  <resume-signal>Type "approved" if helper behavior matches docs, or report mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedule semantics -> helper state | Time-boundary mistakes can mislead automations on appliance timing. |
| helper entity -> dashboards/automations | Wrong idle/active behavior can cause false user actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | I | helper remaining calculation | mitigate | Boundary tests for midnight/weekend/workday and inactive windows. |
| T-09-02 | T | sensor contract drift | mitigate | Extend entity contract tests with unique_id and required attrs. |
| T-09-03 | R | docs/example mismatch | mitigate | Keep README/EXAMPLES parity checks tied to live helper attrs. |
</threat_model>

<verification>
- Targeted gate: `py -m pytest -q tests/test_low_tariff_remaining_sensor.py -x`
- Contract gate: `py -m pytest -q tests/test_entity_presentation_contract.py -k "remaining or helper or parity" -x`
- Safety gate: `py -m pytest -q tests`
</verification>

<acceptance_criteria>
- VADD-01: helper entita korektne ukazuje zostavajuci low-tariff window pre aktivny interval.
- Spravanie je stabilne pri prechodoch dna/vikendu a mimo low tarify je predvidatelne.
- Dokumentacia a test contract reflektuju novu entitu bez regressie existujuceho surface.
</acceptance_criteria>

<success_criteria>
Uzivatel vie v baseline dashboarde bez custom templatu vidiet, kolko casu ostava do konca aktualnej nizkej tarify, s overenou korektnostou na hranicnych casoch.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-low-tariff-helper-entity/09-01-SUMMARY.md`
</output>
