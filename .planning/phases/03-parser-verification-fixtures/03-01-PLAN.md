---
phase: 03-parser-verification-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - tests/conftest.py
  - tests/fixtures/parser/standard_household_business.html
  - tests/fixtures/parser/malformed_js_array.html
  - tests/fixtures/parser/missing_var_name.html
  - tests/test_parser_extract.py
  - tests/test_parser_normalize.py
  - tests/test_parser_get_schedule.py
autonomous: true
requirements:
  - PARS-04
must_haves:
  truths:
    - "Parser fixture test suite runs offline and deterministically without network access."
    - "Common parser inputs produce expected normalized schedule contract for downstream consumers."
    - "Problematic parser inputs fail predictably (`[]` or `None`) and fail tests on contract drift."
  artifacts:
    - path: "pytest.ini"
      provides: "Minimal pytest + pytest-asyncio runner configuration for tests directory."
    - path: "tests/fixtures/parser/*.html"
      provides: "Deterministic local HTML corpus for standard and edge parser scenarios."
    - path: "tests/test_parser_extract.py"
      provides: "Contract checks for `_extract_javascript_array` success/failure semantics."
    - path: "tests/test_parser_normalize.py"
      provides: "Contract checks for `_normalize_schedule` filtering and day-bucket mapping."
    - path: "tests/test_parser_get_schedule.py"
      provides: "Async parser API contract checks with patched `fetch_page` and deterministic assertions."
  key_links:
    - from: "tests/conftest.py"
      to: "tests/fixtures/parser/*.html"
      via: "shared fixture loader"
      pattern: "Path\\(__file__\\).*fixtures/parser"
    - from: "tests/test_parser_get_schedule.py"
      to: "custom_components/zse_hdo/parser.py:get_schedule"
      via: "patched fetch_page + async invocation"
      pattern: "patch.object\\(parser, \"fetch_page\""
    - from: "tests/test_parser_*"
      to: "PARS-04 regression gate"
      via: "pytest assertions on stable output shape and failure contracts"
      pattern: "assert .*\\[\\]|assert .* is None|assert set\\("
---

<objective>
Deliver deterministic parser-fixture verification for `PARS-04` using only local HTML fixtures and a minimal pytest stack.

Purpose: Catch parser regressions before Home Assistant runtime by enforcing explicit parser output and failure contracts from repeatable offline inputs.
Output: Pytest runner config, fixture corpus, and parser contract tests (`extract`, `normalize`, `get_schedule`) with a green automated suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-parser-verification-fixtures/03-CONTEXT.md
@.planning/phases/03-parser-verification-fixtures/03-RESEARCH.md
@.planning/phases/03-parser-verification-fixtures/03-PATTERNS.md
@custom_components/zse_hdo/parser.py
</context>

<read_first>
- Respect locked scope decisions from context: D-01 (`pytest + pytest-asyncio`), D-02 (parser layer only, no HA harness), D-03 (offline fixtures only), D-04 (key fixture cases + output-shape regression gate), D-05 (do not expand into broad corner-case matrix).
- Keep tests deterministic: never call live `zsdis.sk`; patch parser I/O seam (`fetch_page`) for async API tests.
- Assert explicit contracts, not weak smoke checks: required output keys, day-bucket content semantics, and stable failure returns (`[]`, `None`).
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap parser-only test baseline and offline fixture corpus</name>
  <files>pytest.ini, tests/conftest.py, tests/fixtures/parser/standard_household_business.html, tests/fixtures/parser/malformed_js_array.html, tests/fixtures/parser/missing_var_name.html</files>
  <action>Create the minimal test runner baseline per D-01 with `pytest` + `pytest-asyncio` configuration (`asyncio_mode=auto`, `testpaths=tests`) and build fixture-loading helpers in `tests/conftest.py` following the analog in `03-PATTERNS.md` (`tests/conftest.py` -> `custom_components/zse_hdo/parser.py` text-payload flow). For no-analog files (`pytest.ini`, fixture HTML corpus), implement directly from `03-RESEARCH.md` Validation Architecture and deterministic fixture guidance (offline, repeatable corpus) per D-03 and D-04. Do not add Home Assistant harness dependencies or live-network fixture fetch logic per D-02/D-03.</action>
  <verify>
    <automated>py -m pytest --collect-only -q</automated>
  </verify>
  <done>Test discovery succeeds, fixture files are versioned locally, and the repo can collect parser tests without external services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic unit contracts for extract and normalize behavior</name>
  <files>tests/test_parser_extract.py, tests/test_parser_normalize.py</files>
  <behavior>
    - Test 1: `_extract_javascript_array` returns parsed lists for valid `household_rates`/`business_rates` fixture input.
    - Test 2: `_extract_javascript_array` returns `[]` for missing variable and malformed bracket/JSON scenarios.
    - Test 3: `_normalize_schedule` keeps only `t_type == "nt"` entries and maps periods into `workday`/`weekend` buckets from interval flags.
  </behavior>
  <action>Write parser contract tests driven by local fixtures that enforce key extraction and normalization semantics for `PARS-04` per D-04, explicitly mirroring analogs in `03-PATTERNS.md`: `tests/test_parser_extract.py` -> `_extract_javascript_array` deterministic `[]` failure semantics, and `tests/test_parser_normalize.py` -> `_normalize_schedule` `t_type == "nt"` filtering plus `workday`/`weekend` mapping. Use parametrization where helpful, keep assertions explicit for shape/content, and follow `03-RESEARCH.md` deterministic/offline validation guidance.</action>
  <verify>
    <automated>py -m pytest tests/test_parser_extract.py tests/test_parser_normalize.py -q -x</automated>
  </verify>
  <done>Extraction and normalization behavior is covered by deterministic tests that fail on contract regressions in parser helper methods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add async parser API contract tests with patched fetch seam</name>
  <files>tests/test_parser_get_schedule.py</files>
  <behavior>
    - Test 1: `get_schedule` returns expected contract keys (`hdo_number`, `category`, `workday`, `weekend`, `current_tariff`, `source`) for known fixture data.
    - Test 2: `get_schedule` returns `None` for unknown HDO code using the same deterministic fixture payload.
    - Test 3: async tests remain offline by patching `fetch_page` and are robust against wall-clock variance.
  </behavior>
  <action>Implement async parser API tests using `pytest-asyncio` and patched `fetch_page` per D-01/D-03, explicitly following the `03-PATTERNS.md` analog for `tests/test_parser_get_schedule.py` -> `get_schedule` composition/type-normalization/output-shape path in `custom_components/zse_hdo/parser.py`. Stabilize time-sensitive expectations by patching or constraining assertions on `current_tariff` so results are deterministic across execution times (per `03-RESEARCH.md` Common Pitfalls and Validation Architecture). Add final suite-level regression gate command for `PARS-04`.</action>
  <verify>
    <automated>py -m pytest tests/test_parser_extract.py tests/test_parser_normalize.py tests/test_parser_get_schedule.py -q -x</automated>
  </verify>
  <done>Parser public API behavior is regression-gated offline with deterministic async tests and explicit contract assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files -> parser functions | untrusted/synthetic HTML crosses into parser extraction and normalization logic |
| mocked async fetch seam -> parser API | simulated upstream payload enters `get_schedule` contract path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | `tests/fixtures/parser/*.html` + `_extract_javascript_array` | mitigate | Add malformed/missing-variable fixtures and assert deterministic `[]` behavior for parse failures. |
| T-03-02 | T (Tampering) | `get_schedule` payload contract | mitigate | Assert explicit required keys and structural invariants in async contract tests to catch silent shape drift. |
| T-03-03 | D (Denial of Service / availability regression) | parser verification pipeline | mitigate | Enforce fully offline fixture execution with patched `fetch_page` so verification does not fail from external network instability. |
</threat_model>

<verification>
- Primary gate: `py -m pytest tests/test_parser_extract.py tests/test_parser_normalize.py tests/test_parser_get_schedule.py -q -x`
- Secondary gate (full parser fixture suite): `py -m pytest tests -q`
</verification>

<acceptance_criteria>
- `PARS-04` is covered by deterministic fixture tests for both standard and edge parser inputs.
- Parser regressions in output shape or failure semantics break CI/local test execution before HA runtime validation.
- The entire phase remains parser-only and does not introduce Home Assistant harness coupling.
</acceptance_criteria>

<success_criteria>
- Offline fixtures and parser tests execute repeatably with no live HTTP dependency.
- Key parser contracts (`extract`, `normalize`, `get_schedule`) are explicitly asserted and green.
- At least one malformed input and one missing-variable input are enforced as deterministic failure cases.
</success_criteria>

<output>
After completion, create `.planning/phases/03-parser-verification-fixtures/03-01-SUMMARY.md`.
</output>
