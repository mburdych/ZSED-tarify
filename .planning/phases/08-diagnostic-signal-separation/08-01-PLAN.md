---
phase: 08-diagnostic-signal-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/zse_hdo/parser.py
  - custom_components/zse_hdo/coordinator.py
  - custom_components/zse_hdo/sensor.py
  - tests/test_diagnostic_signal_separation.py
autonomous: false
requirements:
  - CONF-03
must_haves:
  truths:
    - "Kazdy failure path ma jednoznacny diagnosticky marker: fetch, parse, alebo tariff-logic."
    - "Diagnosticke metadata su konzistentne medzi parser/coordinator/sensor vrstvou."
    - "Regression test zachyti zmenu alebo odstranenie markerov."
  artifacts:
    - path: custom_components/zse_hdo/parser.py
      provides: "Parser-level error classification marker pre parse failures."
    - path: custom_components/zse_hdo/coordinator.py
      provides: "Fetch/parse error routing a jednotny diagnosticky envelope pre entity payload."
    - path: custom_components/zse_hdo/sensor.py
      provides: "Propagacia diagnostickych markerov do entity atributov bez zmeny existing contract keys."
    - path: tests/test_diagnostic_signal_separation.py
      provides: "Contract tests pre fetch/parse/tariff marker separation a payload consistency."
  key_links:
    - from: custom_components/zse_hdo/coordinator.py
      to: custom_components/zse_hdo/parser.py
      via: "error classification branch for fetch vs parse"
      pattern: "except|UpdateFailed|diagnostic"
    - from: custom_components/zse_hdo/sensor.py
      to: custom_components/zse_hdo/coordinator.py
      via: "extra_state_attributes marker projection"
      pattern: "diagnostic|error_source|error_type"
    - from: tests/test_diagnostic_signal_separation.py
      to: custom_components/zse_hdo/coordinator.py
      via: "forced failure-path assertions"
      pattern: "fetch|parse|tariff"
---

<objective>
Deliver CONF-03 by separating diagnostic signals so logs and entity-visible metadata clearly identify whether failure originated in fetch, parse, or tariff-logic path.

Purpose: Reduce debugging time and eliminate ambiguous "error fetching HDO data" paths for maintainers and users.
Output: Deterministic diagnostic classification contract with automated regression coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@custom_components/zse_hdo/parser.py
@custom_components/zse_hdo/coordinator.py
@custom_components/zse_hdo/sensor.py
</context>

<read_first>
@custom_components/zse_hdo/parser.py
@custom_components/zse_hdo/coordinator.py
@custom_components/zse_hdo/sensor.py
@tests/test_coordinator_reliability.py
@tests/test_sensor_staleness_attrs.py
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing diagnostic contract tests</name>
  <files>tests/test_diagnostic_signal_separation.py</files>
  <behavior>
    - Simulated fetch failure sets marker `diagnostic_error_source=fetch`.
    - Simulated parse failure sets marker `diagnostic_error_source=parse`.
    - Simulated tariff-logic failure sets marker `diagnostic_error_source=tariff_logic`.
    - Marker fields exist in coordinator payload and entity attributes.
  </behavior>
  <action>Create a new focused test module that stubs parser/coordinator execution paths and asserts source-specific marker values instead of generic errors. Keep tests additive-friendly: verify required marker keys and normalized values, not full payload snapshots.</action>
  <verify>
    <automated>py -m pytest -q tests/test_diagnostic_signal_separation.py -x</automated>
  </verify>
  <done>Diagnostic tests fail before implementation and demonstrate the missing separation contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement marker separation in parser and coordinator</name>
  <files>custom_components/zse_hdo/parser.py, custom_components/zse_hdo/coordinator.py</files>
  <action>Introduce a lightweight diagnostic classification envelope that maps error origin to one of `fetch|parse|tariff_logic` and exposes stable fields (`diagnostic_error_source`, `diagnostic_error_code`, `diagnostic_error_at`). Ensure existing reliability metadata (`is_stale`, `last_error_at`, `consecutive_failures`) remains backward compatible and is enriched, not replaced.</action>
  <verify>
    <automated>py -m pytest -q tests/test_diagnostic_signal_separation.py -x</automated>
  </verify>
  <done>Coordinator emits source-specific markers on each failure path while preserving current behavior for success and cache fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Propagate markers through sensor attributes</name>
  <files>custom_components/zse_hdo/sensor.py</files>
  <action>Project diagnostic marker fields into all existing entities via `extra_state_attributes` using the same inclusion pattern as reliability metadata. Do not rename or remove existing attribute keys; only append marker fields if present in coordinator data.</action>
  <verify>
    <automated>py -m pytest -q tests/test_sensor_staleness_attrs.py tests/test_diagnostic_signal_separation.py -x</automated>
  </verify>
  <done>Entity payloads expose diagnostic origin consistently without contract regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual diagnostic readability check in HA logs</name>
  <what-built>Source-separated diagnostic markers and sensor attribute propagation for CONF-03.</what-built>
  <how-to-verify>
    1) V Home Assistant vyvolaj simulovany fetch alebo parse fail (napr. docasne blokovanim zdroja / fixture mismatch).
    2) Over, ze log entry obsahuje jasny marker zdroja (`fetch`, `parse`, alebo `tariff_logic`).
    3) Over v Developer Tools, ze jedna zo ZSE entit ma zodpovedajuci diagnosticky marker v atributoch.
  </how-to-verify>
  <action>Run one manual end-to-end sanity check to confirm operator readability beyond unit tests.</action>
  <verify>
    <automated>py -m pytest -q tests/test_diagnostic_signal_separation.py</automated>
  </verify>
  <done>User confirms marker readability or reports exact mismatch for patch-up.</done>
  <resume-signal>Type "approved" if diagnostics are readable and consistent, or report mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime failures -> diagnostics | Untrusted failures can be misclassified and reduce debuggability. |
| coordinator payload -> dashboard/automation consumers | Marker drift can break operator runbooks and automations relying on root-cause fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | coordinator failure classification | mitigate | Normalize marker values to closed enum (`fetch|parse|tariff_logic`) with test enforcement. |
| T-08-02 | I | sensor attribute projection | mitigate | Append-only marker propagation pattern; retain existing reliability keys unchanged. |
| T-08-03 | R | ambiguous operational logs | mitigate | Emit source-specific diagnostic marker and timestamp for each failure path. |
</threat_model>

<verification>
- Targeted gate: `py -m pytest -q tests/test_diagnostic_signal_separation.py -x`
- Safety gate: `py -m pytest -q tests`
</verification>

<acceptance_criteria>
- CONF-03: fetch, parse a tariff-logic chyby su jednoznacne odlisene v logoch a payload metadata.
- Existing reliability and entity contracts zostavaju kompatibilne (ziadne breaking zmeny atributov).
</acceptance_criteria>

<success_criteria>
Pri incidente maintainer vie bez hadania urcit zdroj chyby z jedinej log/payload inspekcie a regression test to dlhodobo strazi.
</success_criteria>

<output>
After completion, create `.planning/phases/08-diagnostic-signal-separation/08-01-SUMMARY.md`
</output>
