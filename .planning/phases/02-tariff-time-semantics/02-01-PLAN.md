---
phase: 02-tariff-time-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/zse_hdo/time_semantics.py
  - custom_components/zse_hdo/parser.py
autonomous: true
requirements:
  - TIME-01
  - TZ-01
  - CODE-01
must_haves:
  truths:
    - "Aktualny tarif je pocitany konzistentne jednym pravidlom pre lubovolny cas."
    - "Vsetky vypocty v parser vrstve pouzivaju HA timezone-aware cas."
    - "Refaktor nemení existujuci payload kontrakt parsera."
  artifacts:
    - path: "custom_components/zse_hdo/time_semantics.py"
      provides: "Zdielane pure helper funkcie pre day-type, in-period a current tariff vypocet"
    - path: "custom_components/zse_hdo/parser.py"
      provides: "Parser vola zdielanu helper vrstvu a uz nepouziva datetime.now()"
  key_links:
    - from: "custom_components/zse_hdo/parser.py"
      to: "custom_components/zse_hdo/time_semantics.py"
      via: "import + volanie shared helper funkcii"
      pattern: "from \\.time_semantics import"
    - from: "custom_components/zse_hdo/parser.py"
      to: "homeassistant.util.dt"
      via: "dt_util.now() pre last_updated a runtime vypocty"
      pattern: "dt_util\\.now\\("
---

<objective>
Zaviest jednotnu helper vrstvu pre tariff/midnight semantiku a migrovat parser na HA timezone-aware cas.

Purpose: Uzatvara CODE-01 a TZ-01 v parser vrstve bez zmeny existujuceho output kontraktu.
Output: Novy `time_semantics.py` + parser upraveny na shared helper API.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-tariff-time-semantics/02-CONTEXT.md
@.planning/phases/02-tariff-time-semantics/02-RESEARCH.md
@custom_components/zse_hdo/parser.py
@custom_components/zse_hdo/sensor.py
@custom_components/zse_hdo/coordinator.py

<interfaces>
From `custom_components/zse_hdo/parser.py`:
- `def _normalize_schedule(self, intervals: List[Dict]) -> Dict[str, List[Dict]]`
- `def _calculate_current_tariff(self, schedule: Dict[str, List[Dict]]) -> str`
- `async def get_schedule(self, hdo_number: int) -> Optional[Dict]`
- `async def is_low_tariff_now(self, hdo_number: int) -> Optional[bool]`

From `custom_components/zse_hdo/coordinator.py`:
- `from homeassistant.util import dt as dt_util`
- `now = dt_util.now()` (timezone-aware standard in this integration)

Locked decisions to implement:
- D-01, D-02, D-03, D-04, D-05 from `02-CONTEXT.md`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared tariff time semantics module</name>
  <read_first>
    - .planning/phases/02-tariff-time-semantics/02-CONTEXT.md
    - .planning/phases/02-tariff-time-semantics/02-PATTERNS.md
    - custom_components/zse_hdo/parser.py
    - custom_components/zse_hdo/sensor.py
  </read_first>
  <files>custom_components/zse_hdo/time_semantics.py</files>
  <behavior>
    - Given a schedule and HA-local datetime, helper returns `low` only when current time falls inside matching low-tariff interval.
    - Midnight-crossing interval (`end < start`) uses inclusive-or rule (`>= start OR < end`) exactly once in shared code.
    - Day-type selection uses candidate datetime weekday (not global mutable state), preserving weekend/workday semantics.
  </behavior>
  <action>Create `custom_components/zse_hdo/time_semantics.py` with pure helper functions used by parser and sensors (per D-03), explicitly following analog mappings in `02-PATTERNS.md`: **Midnight-crossing tariff predicate**, **Day-type branch (workday vs weekend)**, and **Stable payload/entity contract**. Implement parse time helpers, select day periods from schedule by supplied datetime, evaluate in-period with midnight crossover, compute current tariff string (`low`/`high`), and compute next-switch payload compatible with existing sensor attribute contract. Use `dt_util.now()` as default time source for helpers that accept optional `now` (per D-01, D-02, D-04), aligned with the **Home Assistant timezone source** pattern. Keep user-facing semantics unchanged (per D-05).</action>
  <verify>
    <automated>python -m py_compile custom_components/zse_hdo/time_semantics.py</automated>
  </verify>
  <acceptance_criteria>Module exists, is importable, contains one canonical midnight predicate, and exposes helpers required by parser + sensor call sites.</acceptance_criteria>
  <done>Shared helper module compiles and can evaluate current tariff + next switch without touching entity contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate parser to shared helper and HA clock</name>
  <read_first>
    - custom_components/zse_hdo/time_semantics.py
    - custom_components/zse_hdo/parser.py
    - .planning/phases/02-tariff-time-semantics/02-RESEARCH.md
  </read_first>
  <files>custom_components/zse_hdo/parser.py</files>
  <behavior>
    - `get_schedule()` still returns same keys/shape as before.
    - `current_tariff` in parser payload is produced via shared helper, not duplicated inline logic.
    - Parser no longer uses `datetime.now()` for tariff logic or last_updated timestamp generation.
  </behavior>
  <action>Update parser imports to use `homeassistant.util.dt as dt_util` and functions from `time_semantics.py`. Replace `_calculate_current_tariff` and `is_low_tariff_now` internals so they delegate to shared helper logic (per D-03), explicitly mapped to `02-PATTERNS.md` analogs: **Midnight-crossing tariff predicate**, **Day-type branch (workday vs weekend)**, **Stable payload/entity contract**, and **Home Assistant timezone source**. Replace remaining `datetime.now()` reads with `dt_util.now()` (per D-01). Preserve return schema, Slovak user-facing text, and key names (`current_tariff`, `workday`, `weekend`, `last_updated`) exactly (per D-05).</action>
  <verify>
    <automated>if rg -n "datetime\.now\(" custom_components/zse_hdo/parser.py; then echo "FAIL: datetime.now() still present in parser.py"; exit 1; else echo "PASS: no datetime.now() found in parser.py"; fi && python -m py_compile custom_components/zse_hdo/parser.py custom_components/zse_hdo/time_semantics.py && python custom_components/zse_hdo/parser.py</automated>
  </verify>
  <acceptance_criteria>`rg` finds zero parser runtime usages of `datetime.now()`, parser smoke run completes, and output payload keys remain backward-compatible.</acceptance_criteria>
  <done>Parser uses shared semantics + HA timezone-aware time source while preserving existing external behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ZSE HTML -> parser normalization | Untrusted schedule data enters tariff calculation path |
| Parsed schedule -> time semantics helper | Malformed interval fields can affect low/high decision output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `time_semantics.py` interval evaluator | mitigate | Validate required interval fields before evaluation and fall back to safe `high` when data is invalid |
| T-02-02 | D | `parser.py` tariff computation path | mitigate | Keep parser resilient: preserve existing exception handling and avoid crash on malformed interval parse |
| T-02-03 | R | `parser.py` migration traceability | accept | Existing integration logs already capture fetch/parse errors; no new audit trail requirement in this phase |
</threat_model>

<verification>
- `python -m py_compile custom_components/zse_hdo/time_semantics.py custom_components/zse_hdo/parser.py`
- `rg "datetime\.now\(" custom_components/zse_hdo/parser.py`
- `python custom_components/zse_hdo/parser.py`
</verification>

<success_criteria>
- Shared helper is the single source for parser tariff boundary logic (CODE-01 part).
- Parser uses HA-aware time source consistently (TZ-01 part).
- Existing parser payload contract and low/high semantics remain unchanged (TIME-01 preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/02-tariff-time-semantics/02-01-SUMMARY.md`
</output>
