---
phase: 02-tariff-time-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - custom_components/zse_hdo/sensor.py
  - custom_components/zse_hdo/time_semantics.py
autonomous: false
requirements:
  - TIME-02
  - TIME-03
  - TZ-01
  - CODE-01
must_haves:
  truths:
    - "Sensor aktualnej tarify zobrazuje spravny stav pri prechode cez polnoc."
    - "Next-switch senzor vracia najblizsi realny prechod aj cez zmenu dna a day-type."
    - "Senzory pouzivaju rovnaky vypocet ako parser (jedno miesto pravdy)."
  artifacts:
    - path: "custom_components/zse_hdo/sensor.py"
      provides: "Senzory deleguju time-semantics logiku na shared helper vrstvu"
    - path: "custom_components/zse_hdo/time_semantics.py"
      provides: "Doplnena helper API pre next switch a day-type metadata pouzivana senzormi"
  key_links:
    - from: "custom_components/zse_hdo/sensor.py"
      to: "custom_components/zse_hdo/time_semantics.py"
      via: "shared helper calls in tariff and next-switch sensors"
      pattern: "time_semantics"
    - from: "custom_components/zse_hdo/sensor.py"
      to: "homeassistant.util.dt"
      via: "timezone-aware now source in entity computations"
      pattern: "dt_util\\.now\\("
---

<objective>
Migrovat senzory na zdielanu tariff semantiku a potvrdit korektnost hraničných casovych scenarov v HA runtime.

Purpose: Dokoncit Phase 2 closure pre TIME-02/TIME-03/TZ-01/CODE-01 bez regresie user-facing atributov.
Output: Refaktorovane `sensor.py` + overene boundary spravanie v parser smoke a manual HA verify.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-tariff-time-semantics/02-CONTEXT.md
@.planning/phases/02-tariff-time-semantics/02-VALIDATION.md
@.planning/phases/02-tariff-time-semantics/02-01-SUMMARY.md
@custom_components/zse_hdo/sensor.py
@custom_components/zse_hdo/time_semantics.py

<interfaces>
From `custom_components/zse_hdo/sensor.py` (contracts to preserve):
- `class ZSEHDOTariffSensor(CoordinatorEntity, BinarySensorEntity)` -> `is_on`, `extra_state_attributes`
- `class ZSEHDONextSwitchSensor(CoordinatorEntity, SensorEntity)` -> `_get_next_switch`, `native_value`, `extra_state_attributes`
- `class ZSEHDOTodayScheduleSensor(CoordinatorEntity, SensorEntity)` -> `native_value`, `extra_state_attributes`

Contract-sensitive attributes to keep unchanged (D-05):
- `tariff_name`, `time`, `to_tariff`, `to_tariff_name`, `day_type`, `periods`, `period_count`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace sensor-local time logic with shared helpers</name>
  <read_first>
    - .planning/phases/02-tariff-time-semantics/02-01-SUMMARY.md
    - custom_components/zse_hdo/sensor.py
    - custom_components/zse_hdo/time_semantics.py
    - .planning/phases/02-tariff-time-semantics/02-CONTEXT.md
  </read_first>
  <files>custom_components/zse_hdo/sensor.py, custom_components/zse_hdo/time_semantics.py</files>
  <behavior>
    - Tariff sensor `is_on` uses shared helper decision path; no duplicated midnight predicate in sensor class.
    - Next-switch sensor uses shared helper and still returns same attribute names and datetime ISO output.
    - Today-schedule sensor day-type selection uses HA-local datetime semantics consistently.
  </behavior>
  <action>Refactor `sensor.py` to import shared helper functions and remove duplicated `_parse_time` and interval-branch logic from `ZSEHDOTariffSensor` and `ZSEHDONextSwitchSensor` (per D-03, CODE-01). Implement this explicitly using assigned analog patterns from `02-PATTERNS.md`: **Midnight-crossing tariff predicate** (Shared Patterns), **Day-type branch (workday vs weekend)**, and **Stable payload/entity contract**; keep helper shape consistent with `sensor.py` primary analog and parser compatibility notes. Replace sensor `datetime.now()` reads with `dt_util.now()` using the **Home Assistant timezone source** shared pattern (per D-01, TZ-01). If helper API adjustments are required for clean sensor integration, extend `time_semantics.py` in a backward-compatible way while keeping parser integration intact. Preserve all existing entity IDs/names/attributes and Slovak labels exactly (per D-05).</action>
  <verify>
    <automated>if rg -n "datetime\.now\(" custom_components/zse_hdo/sensor.py; then echo "FAIL: datetime.now() still present in sensor.py"; exit 1; else echo "PASS: no datetime.now() found in sensor.py"; fi && python -m py_compile custom_components/zse_hdo/sensor.py custom_components/zse_hdo/time_semantics.py && python custom_components/zse_hdo/parser.py</automated>
  </verify>
  <acceptance_criteria>`sensor.py` contains no tariff/midnight duplication blocks, no runtime `datetime.now()` usage, and compiles with shared helper imports.</acceptance_criteria>
  <done>All tariff-time decisions in sensors flow through shared helper logic while entity contract remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Run deterministic boundary verification commands</name>
  <read_first>
    - .planning/phases/02-tariff-time-semantics/02-VALIDATION.md
    - custom_components/zse_hdo/parser.py
    - custom_components/zse_hdo/sensor.py
  </read_first>
  <files>custom_components/zse_hdo/sensor.py</files>
  <action>Execute regression-oriented CLI checks tied to phase requirements: parser smoke run, duplicate-logic scan, and timezone-source scan. Record command outputs in plan summary, explicitly confirming parity for midnight crossover and weekend/workday branch behavior (TIME-03) and next-switch logic path (TIME-02).</action>
  <verify>
    <automated>python -m py_compile custom_components/zse_hdo/parser.py custom_components/zse_hdo/sensor.py custom_components/zse_hdo/time_semantics.py && rg "_calculate_current_tariff|_get_next_switch|datetime\.now\(" custom_components/zse_hdo && python custom_components/zse_hdo/parser.py</automated>
  </verify>
  <acceptance_criteria>Smoke run passes and grep output confirms single shared logic path and HA time-source adoption across parser/sensor modules.</acceptance_criteria>
  <done>Automated checks provide concrete evidence that Phase 2 technical goals are satisfied before human sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify HA runtime behavior around boundary transitions</name>
  <read_first>
    - .planning/phases/02-tariff-time-semantics/02-VALIDATION.md
    - custom_components/zse_hdo/sensor.py
  </read_first>
  <what-built>Parser and sensors now share one timezone-aware tariff semantics implementation.</what-built>
  <action>Perform final runtime confirmation in a Home Assistant dev instance after Task 1 and Task 2 automation completes, and validate that real entity state transitions match expected schedule behavior at boundary times before phase closure.</action>
  <how-to-verify>
    1) Restart Home Assistant dev instance with updated integration.
    2) Open entities for one configured HDO and inspect `binary_sensor` tariff + `next_switch` sensor.
    3) Validate behavior in two edge windows: near midnight crossover interval and weekend/workday boundary.
    4) Confirm attributes (`tariff_name`, `time`, `to_tariff_name`, `day_type`, `period_count`) are unchanged in naming and meaning.
  </how-to-verify>
  <verify>
    <automated>python -m py_compile custom_components/zse_hdo/sensor.py custom_components/zse_hdo/time_semantics.py && python custom_components/zse_hdo/parser.py</automated>
  </verify>
  <acceptance_criteria>User confirms observed HA entity behavior matches expected schedule transitions with no contract regressions.</acceptance_criteria>
  <done>Human verification confirms boundary correctness and entity contract stability, allowing phase execution to proceed to completion.</done>
  <resume-signal>Type "approved" to finalize phase, or provide mismatch details to continue fixes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coordinator cached schedule -> sensor state | Untrusted/changed schedule data directly affects user-visible tariff states |
| Helper output -> HA entity attributes | Wrong switch prediction propagates to automations and dashboards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `sensor.py` helper integration | mitigate | Remove duplicated branches and route all checks through one helper implementation |
| T-02-05 | I | `next_switch` output attributes | mitigate | Preserve attribute schema and verify with runtime checkpoint before close |
| T-02-06 | D | Boundary edge handling | mitigate | Execute parser smoke + boundary scenario verification prior to approval |
</threat_model>

<verification>
- `python -m py_compile custom_components/zse_hdo/sensor.py custom_components/zse_hdo/time_semantics.py`
- `python custom_components/zse_hdo/parser.py`
- `rg "_calculate_current_tariff|_get_next_switch|datetime\.now\(" custom_components/zse_hdo`
- Human checkpoint in HA dev instance for midnight + weekend boundary behavior.
</verification>

<success_criteria>
- Next-switch and current tariff remain correct across midnight/weekend boundaries (TIME-02, TIME-03).
- Sensor and parser runtime clock source is HA `dt_util`-based (TZ-01).
- Tariff logic duplication is removed and centralized (CODE-01).
</success_criteria>

<output>
After completion, create `.planning/phases/02-tariff-time-semantics/02-02-SUMMARY.md`
</output>
